
create table ulke (ulke_id integer auto_increment primary key not null,name varchar(50));
-- drop table ulke;
-- drop table il;
create table il (il_id integer auto_increment primary key not null,name varchar(50),ulke_id int not null,FOREIGN KEY (ulke_id) REFERENCES ulke(ulke_id));

insert into ulke (name) values('Turkey');
insert into il (name,ulke_id) values('Istanbul',1);
insert into il (name,ulke_id) values('Adana',1);
insert into il (name,ulke_id) values('Izmir',1);
insert into il (name,ulke_id) values('Ankara',1);
insert into il (name,ulke_id) values('Bursa',1);
insert into il (name,ulke_id) values('Kahraman Marash',1);

https://gitee.com/y_project/RuoYi
https://gitee.com/liujpts/ruoyi-ui

run ruoyi and do following to generate code and how to use it in the system.
make sure to use java 8 openjdk by Inteleji>File>Project Structure>SDK
go to ruoyi-ui and write command npm run dev use > node version 12.22.12
username and password: admin, admin123
start redis server
start RuoyiApplication as debug
code generate> api under ruoyi-ui>api>createfolder> index.js , under view forlder create folder and name file index.vue and past the code
and others create file as said from back end place it where its said.
ruoyi-system>src>main>java>com.ruoyi.system>domain
similar with mapper,service and service>impl
and then controller create folder inside ruoyi-admin>src>main>java>com.ruoyi>web>controller>foldername>createcontroler
and about mapper.xml
create under ruoyi-stem>src>main>resources>mapper.system>filename.xml
and then create menu in the rouye front end
system managment>menu managment> select parent menu you want to place and then meue type MENU and menu icone and then menu name and menu sort which place you want put in sort in number like 11 or 12 etc.
two imp componante path ulke/index.js and permission character system:ulke:list
and dont forget to change the ruoyi-ui>src>views>ulke>index.vue change the @api/system/ulke to @api/ulke

you generte the jars and run using java -jar admin.jar
